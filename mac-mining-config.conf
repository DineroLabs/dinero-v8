# Dinero Mining Configuration
# Mac → mines blocks
# Server → validates and stores blockchain

# Connect to server node for blockchain data
rpcconnect=96.9.226.98
rpcport=20998

# Enable mining on Mac
gen=1
genproclimit=4

# Don't listen for P2P (server does that)
listen=0

# Use local data directory for mining state only
datadir=/Users/haydarevich/Documents/DineroCoin/data-mining

